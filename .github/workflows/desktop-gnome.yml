# Desktop smoketest — push, PR, weekly, manual.

name: Desktop / GNOME

on:
  push:
    branches: [main, master]
  pull_request:
  schedule:
    - cron: "20 7 * * 1"
  workflow_dispatch:

concurrency:
  group: desktop-gnome-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    uses: ./.github/workflows/smoketest-reusable.yml
    with:
      desktop: GNOME
      minimal_xorg: false
      mem: "12288"
      timeout_minutes: 180
