name: Desktop / LXDE

on:
  push:
    branches: [main, master]
  pull_request:
  schedule:
    - cron: "20 6 * * 1"
  workflow_dispatch:

concurrency:
  group: desktop-lxde-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    uses: ./.github/workflows/smoketest-reusable.yml
    with:
      desktop: LXDE
      minimal_xorg: true
      mem: "8192"
      timeout_minutes: 120
